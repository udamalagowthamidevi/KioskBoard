# KioskBoard
 Built a self-service kiosk and real-time order tracking system for quick-service restaurants using Spring Boot, JPA, and MySQL, enabling idempotent order creation and live status updates via WebSockets (STOMP).
